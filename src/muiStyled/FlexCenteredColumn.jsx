import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexCenteredColumn = styled(Box)({
  display: "flex",
  justifyContent: "flex-start",
  alignItems: "center",
  flexDirection: "column",
});

export default FlexCenteredColumn;
