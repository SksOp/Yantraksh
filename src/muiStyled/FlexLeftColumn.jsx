import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexLeftColumn = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "flex-start",
  flexDirection: "column",
});

export default FlexLeftColumn;
