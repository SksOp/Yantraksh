import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexCenteredRow = styled(Box)({
  display: "flex",
  justifyContent: "space-around",
  alignItems: "center",
});

export default FlexCenteredRow;
